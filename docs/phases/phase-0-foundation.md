# Phase 0 Report: Research Charter, Governance, Recovery, and Project Foundation

## Phase

Phase 0 -- Research Charter, Governance, Recovery, and Project Foundation.

## Objective

Establish a recoverable research project foundation for Juniper Auto before
any expensive model, tokenizer, dataset, runtime, or training work begins:
research charter, governance, frozen architecture/dense-control
configuration with independently-derived parameter accounting, environment
specification and dependency locking, deterministic seeding, structured
logging, an experiment registry, time accounting, an ADR system, a phase
report system, artifact manifests and hashes, automated tests, CI, a single
validation entrypoint, recovery documentation, and a genuinely executed
clean-clone recovery exercise.

## Starting commit

`a991db7fb302d68f2eff7421fc29890928e02ec3` -- the pre-existing "Initial
commit" on `github.com/Cinqic/Juniper-Auto` (`main`), containing only
`LICENSE` (MIT) and a one-line `README.md`. Confirmed via repository
reconnaissance before any Phase 0 file was written.

## Final commit

`PENDING_CANDIDATE_COMMIT` -- filled in at the end of this engineering
session, immediately before push. See "Approval status" below.

## Implementation summary

- **Research charter and governance:**
  [docs/research/project-charter.md](../research/project-charter.md),
  [docs/research/project-governance.md](../research/project-governance.md)
  (39 operational governance rules, each with a stated operational
  consequence).
- **Frozen architecture configuration:**
  [configs/architecture/ja150m-v0.1.yaml](../../configs/architecture/ja150m-v0.1.yaml)
  and
  [ja150m-v0.1-dense.yaml](../../configs/architecture/ja150m-v0.1-dense.yaml),
  schema-validated by `juniper_auto/config/schema.py` (Pydantic v2, strict
  models, cross-field validators for layer partition, head divisibility,
  top-k bounds, dropless-routing enforcement) and frozen-value-checked by
  `juniper_auto/config/frozen.py`.
- **Programmatic parameter accounting:**
  `juniper_auto/accounting/parameter_count.py` independently derives every
  component count from the loaded config (embeddings, attention, dense
  FFNs, routed/shared experts, routers, QK norms, block norms, final norm)
  and sums to the exact frozen totals: 150,031,360 total / 79,252,480
  standard active (sparse), 79,191,040 (dense control).
- **Deterministic foundation, seeding, logging:** `juniper_auto/util/seed.py`
  (explicit-seed-only, CPU/CUDA, honest about determinism limits),
  `juniper_auto/util/logging.py` (structured JSON logging),
  `juniper_auto/foundation/probe.py` (`FoundationProbe` -- explicitly not
  the Juniper Auto model).
- **ADRs:** [docs/adr/](../adr/) -- 6 ADRs covering the one-model
  architecture, dense-control requirement, config format/validation choice,
  dependency-locking approach, repository canonicality, and
  validation/recovery strategy.
- **Experiment registry:** [experiments/registry.yaml](../../experiments/registry.yaml)
  with 2 Phase 0 foundation-validation entries (parameter-accounting
  verification, foundation-probe determinism check).
- **Time accounting:** [docs/time/phase-hours.csv](../time/phase-hours.csv).
- **Manifests:** [manifests/frozen-artifacts.yaml](../../manifests/frozen-artifacts.yaml)
  (architecture frozen; tokenizer/datasets/checkpoints/runtime honestly
  `not-yet-created`), [manifests/phase-0-artifact-hashes.yaml](../../manifests/phase-0-artifact-hashes.yaml)
  (SHA-256 over 9 critical Phase 0 artifacts, generated by
  `scripts/hash_manifest.py`).
- **Tests:** `tests/` -- config validation, parameter accounting, frozen
  architecture constants, imports, determinism, manifests, hashes, time
  schema, repository integrity, absolute-path checks.
- **CI:** `.github/workflows/phase-0-validation.yml`, `ubuntu-latest`,
  CPU-only.
- **Validator:** `scripts/validate_repo.py --all`.
- **Recovery:** [docs/recovery/README.md](../recovery/README.md), executed
  as a genuine fresh-clone/fresh-venv exercise (see "Recovery status"
  below).
- **README:** repository root, describes current state without overclaiming.

## Architecture / configuration IDs

`ja150m-v0.1` (sparse), `ja150m-v0.1-dense` (dense control). Both frozen for
this phase; see [ADR-0001](../adr/0001-one-model-moe-architecture.md) and
[ADR-0002](../adr/0002-dense-control-requirement.md).

## Environment

[docs/architecture/environment-specification.md](../architecture/environment-specification.md).
Development on FLOWBOX (Ryzen 7 5700G, RTX 2060 6GB, Linux Mint 22.3,
Python 3.12.3); CI on GitHub Actions `ubuntu-latest`, CPU-only.

## Artifacts

See [manifests/frozen-artifacts.yaml](../../manifests/frozen-artifacts.yaml)
for full status. Frozen this phase: `ja150m-v0.1` architecture,
`ja150m-v0.1-dense` architecture. Everything else (tokenizer, datasets,
checkpoints, runtime protocol, tool/memory/state schemas, permission
policy, evaluation suite) is honestly `not-yet-created`.

## Hashes

[manifests/phase-0-artifact-hashes.yaml](../../manifests/phase-0-artifact-hashes.yaml),
SHA-256, 9 artifacts (both architecture configs, charter, governance,
dependency lock, accounting source, config schema, config frozen-value
module, `scripts/validate_repo.py`). Verified by
`tests/test_hashes.py` and the "artifact hashes" gate in
`scripts/validate_repo.py`.

## Tests

`pytest tests/ -q` -- see command output recorded in this session; result
summary filled in under "Final local validation result" once the candidate
is frozen. All tests pass as of the last recorded run before candidate
commit (see below).

## Evaluations

Not applicable. No model exists to evaluate in Phase 0.

## Ablations (where relevant)

Not applicable.

## CI workflow / run

Workflow: `Phase 0 Validation` (`.github/workflows/phase-0-validation.yml`).
Run ID, commit tested, and conclusion: `PENDING_CI_VERIFICATION` -- filled
in after the candidate commit is pushed and CI completes.

## Recovery status

`PENDING` -- to be filled in immediately below after the isolated
fresh-clone/fresh-venv recovery exercise is executed (Step 25 of the Phase
0 engineering instructions). See
[docs/recovery/README.md](../recovery/README.md) for the procedure being
tested.

## Engineering hours

See [docs/time/phase-hours.csv](../time/phase-hours.csv), `phase-0` rows.
Filled in at candidate-commit time.

## Independent review hours

`0` / pending. GPT-5.6 Sol has not yet reviewed this repository.

## GPU hours

`0`. Phase 0 performs no training or GPU-bound work.

## CPU / data-processing hours

`~0`. Phase 0 performs no dataset processing; CPU time was spent on
dependency installation and running the test/validation suite, not
recorded as a separate "data-processing" category since no data exists
yet.

## Project elapsed days

`0` days -- Phase 0 engineering began and (candidate) completed on the same
calendar day the canonical repository's initial commit was made
(2026-08-25).

## Known failures

None outstanding at candidate-commit time. See "Self-Review Fix Cycle" in
`docs/phases/phase-0-sonnet-self-review.md` for issues found and fixed
during self-review.

## Negative results

None recorded yet -- `experiments/registry.yaml` contains only
`status: completed` foundation-validation entries for Phase 0. No
experiment has failed or produced a negative result at this stage.

## Accepted limitations

- License selection beyond the pre-existing MIT `LICENSE` is an open
  governance item, not decided by Sonnet (see
  [project-charter](../research/project-charter.md), License status).
- CUDA/GPU bitwise determinism is not claimed or tested -- only best-effort
  CPU determinism (`juniper_auto/util/seed.py`).
- The clean-clone recovery exercise is an isolated fresh-clone/fresh-venv
  test plus clean remote Linux CI, not a literal from-scratch OS
  reinstallation of the physical development machine.
- `nvcc`/system CUDA toolkit is not installed on FLOWBOX; PyTorch's bundled
  CUDA runtime libraries are relied on instead (see environment
  specification).
- No tokenizer, dataset, checkpoint, or runtime exists yet -- by design,
  this is Phase 1+ scope.

## Reproducibility procedure

[docs/recovery/README.md](../recovery/README.md), Steps 1-6. Summary:
`git clone` -> `python3 -m venv .venv` -> `pip install -r
requirements-lock.txt` -> `pip install -e . --no-deps` ->
`python scripts/validate_repo.py --all`.

## Reviewer identity

Self-review: Claude Sonnet 5 (implementer) -- see
[docs/phases/phase-0-sonnet-self-review.md](phase-0-sonnet-self-review.md).
Independent review: GPT-5.6 Sol, not yet performed.

## Approval status

`CANDIDATE - PENDING INDEPENDENT REVIEW`
